// 8.a) create an array fruits
const fruits=['apple', 'banana', 'orange'];

// b) Add grapes to the fruits array
fruits.push('grapes');
console.log(fruits)

// c) Remove the first fruit
fruits.shift('apple');
console.log(fruits)

// d)  Access the second element in the array
fruits.slice(-2,[0]);
console.log(fruits)
